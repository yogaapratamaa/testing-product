import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    line: {
        boxShadow: 'none',
    },
    bar: {
        boxShadow: 'none',
    },
}));

export default useStyles;
